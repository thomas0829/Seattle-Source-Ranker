[pytest]
# Minimal pytest configuration without ROS interference

python_files = test_*.py
python_classes = Test*
python_functions = test_*

testpaths = test

addopts = -v --tb=short

[tool:pytest]
norecursedirs = .git .tox dist build *.egg __pycache__
